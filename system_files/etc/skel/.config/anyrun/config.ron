Config(
  x: Fraction(0.5),
  y: Fraction(0.3),
  width: Absolute(800),
  height: Absolute(0),
  hide_icons: false,
  ignore_exclusive_zones: false,
  layer: Overlay,
  hide_plugin_info: true,
  close_on_click: true,
  show_results_immediately: false,
  max_entries: None,
  plugins: [
    "/usr/lib64/anyrun/libapplications.so",
    "/usr/lib64/anyrun/libsymbols.so",
    "/usr/lib64/anyrun/libshell.so",
  ],
)
