# Override default home.conf to prevent 'already exists and is not a directory' errors
# in OSTree/Bootable OCI systems where these paths are symlinks to /var
L /home - - - - var/home
L /srv - - - - var/srv
L /root - - - - var/roothome
