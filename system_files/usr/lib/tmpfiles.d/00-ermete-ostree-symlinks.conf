# 00-ermete-ostree-symlinks.conf
# Override delle direttive di systemd per evitare avvisi (warnings)
# "already exists and is not a directory" su percorsi gestiti da ostree.

L+ /home - - - - /var/home
L+ /root - - - - /var/roothome
L+ /srv - - - - /var/srv
