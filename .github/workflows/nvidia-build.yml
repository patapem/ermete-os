name: NVIDIA kmod build

# The NVIDIA kernel modules (forge/specs/azoth/nvidia.sh, spec section 10) against a
# kernel-devel, on the self-hosted runner with the kernel toolchain. Reusable workflow:
# Kernel Build calls it for the gate on PRs and pushes (the kernel-devel of the freshly
# built artifact, or of the published image when the kernel is reused) and NVIDIA kmod
# calls it for the signing and publication chain. Uploads the nvidia-<branch>-unsigned
# artifacts.

on:
  workflow_call:
    inputs:
      devel-artifact:
        description: "artifact holding kernel-devel-*.rpm; empty: the azoth-devel:<nvr> image published for the pins"
        type: string
        default: ""

permissions:
  contents: read

env:
  KERNEL_IMAGE: ghcr.io/${{ github.repository_owner }}/azoth

jobs:
  build:
    runs-on: self-hosted
    timeout-minutes: 60
    permissions:
      contents: read
      packages: read
    strategy:
      fail-fast: false
      matrix:
        driver: [open, legacy]
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Build the nvidia image
        env:
          BUILDAH_ISOLATION: chroot
        run: podman build -t localhost/azoth-nvidia forge/specs/azoth/nvidia

      - name: Kernel-devel from the artifact
        if: ${{ inputs.devel-artifact != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: ${{ inputs.devel-artifact }}
          path: devel

      - name: Kernel-devel from the published image
        # The NVR comes from the pins (nvr.sh): the kernel built against is the published
        # one. Login with the job token: the package is public, but the podman of a
        # self-hosted runner may hold stale ghcr credentials in auth.json, and ghcr answers
        # 403 to the anonymous token. Logout at the end of the step, so none are left.
        if: ${{ inputs.devel-artifact == '' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          nvr=$(bash forge/specs/azoth/nvr.sh)
          echo "${GH_TOKEN}" | podman login ghcr.io -u "${GITHUB_ACTOR}" --password-stdin
          trap 'podman logout ghcr.io' EXIT
          ctr=$(podman create "${KERNEL_IMAGE}-devel:${nvr}" /kernel-devel)
          mkdir -p devel && podman cp "${ctr}:/." devel/ && podman rm "$ctr" > /dev/null

      - name: Run nvidia.sh build
        run: |
          set -euo pipefail
          ls devel
          mkdir -p "$HOME/.cache/azoth" out
          podman run --rm \
            -v "$GITHUB_WORKSPACE:/forge" \
            -v "$HOME/.cache/azoth:/var/cache/azoth" \
            -w /forge localhost/azoth-nvidia \
            bash forge/specs/azoth/nvidia.sh build --driver "${{ matrix.driver }}" --devel /forge/devel --out /forge/out

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: nvidia-${{ matrix.driver }}-unsigned
          path: out/
          if-no-files-found: error
