# Patches from CachyOS/kernel-patches (commit CACHYOS_PATCHES_COMMIT of pins.env), paths
# relative to the series directory (7.1/...). build.sh applies them in this order on top
# of the CachyOS base with git apply: a hunk that does not apply stops the build.
sched/0001-bore-cachy.patch
